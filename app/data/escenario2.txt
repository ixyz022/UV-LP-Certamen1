CAPA Capa1 {
    CELDA Celda1 (SUSCEPTIBLE);
    CELDA Celda2 (INFECTADO);
    CELDA Celda3 (RECUPERADO);
}

REGLA SUSCEPTIBLE -> INFECTADO SI INFECTADO EN { Celda1 }
REGLA INFECTADO -> RECUPERADO SI RECUPERADO EN { Celda2 }
REGLA INFECTADO -> MUERTO SI MUERTO EN { Celda2 }

SIMULACION {
    PASO {
        Celda1 -> INFECTADO;
        Celda2 -> RECUPERADO;
    }
}
